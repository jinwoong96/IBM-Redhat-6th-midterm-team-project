import React from 'react';

const FormComment = ({content}) => {
    return (
        <p className="mt-2 text-sm text-gray-500">
            {content}
        </p>
    );
};

export default FormComment;