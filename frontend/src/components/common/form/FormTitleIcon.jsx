import React from 'react';

const FormTitleIcon = ({content}) => {
    return (
        <div className="mb-4 flex h-16 w-16 items-center justify-center rounded-2xl bg-blue-100">
            {content}
        </div>
    );
};

export default FormTitleIcon;